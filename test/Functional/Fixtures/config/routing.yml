property_route:
    path: /post/{title}

method_route:
    path: /post/{slug}

mapped_property_route:
    path: /post/{title_slug}

mapped_fallback_route:
    path: /post/{post}

client_route:
    path: /client/{client}

authentication_route:
    path: /authentication/{authentication}

message_route:
    path: /message/{message}

login:
    path: /login
